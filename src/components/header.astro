---
import type { CollectionEntry } from "astro:content";

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<header class="w-screen p-4 bg-primary-200 text-center">
    <h1 class="font-bold text-2xl">{title}</h1>
</header>
