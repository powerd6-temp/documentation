---
import type { CollectionEntry } from "astro:content";

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<header class="w-full p-4 bg-primary-200 flex justify-stretch items-center">
    <a class="text-xs flex-grow-0" href="/">home</a>
    <h1 class="font-bold text-2xl mx-auto">{title}</h1>
</header>
