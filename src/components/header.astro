---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<header class="w-full p-4 py-3 sm:py-6 bg-primary-200 text-center">
    <h1 class="font-bold text-2xl mx-auto">{title}</h1>
</header>
